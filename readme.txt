To successfully run the code please follow these steps:

step 1: Run the command on the ternimal (npm i)
step 2: Create the database on mongoDB with the name login-app-db
step 3: Run the command on the ternimal (node server.js)
step 4: Start the server on http://127.0.0.1:3000